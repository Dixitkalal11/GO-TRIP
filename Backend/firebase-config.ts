// Firebase Admin SDK Configuration - TypeScript version
import * as admin from 'firebase-admin';

let firebaseApp: admin.app.App | null = null;

console.log('Firebase Admin SDK disabled - using direct user info');

export default firebaseApp;














