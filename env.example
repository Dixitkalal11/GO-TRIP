# Example environment variables for GO-TRIP
# Copy this file to `.env` and fill in real values. DO NOT commit real secrets.

# Server
PORT=5000
NODE_ENV=development

# Application secrets
JWT_SECRET=your-jwt-secret-here

# Database (replace or update Backend/src/config/database.js to use these vars)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=gotrips
DB_USER=root
DB_PASSWORD=

# Razorpay (payment)
RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID
RAZORPAY_KEY_SECRET=YOUR_KEY_SECRET

# Email / SMTP (for sending emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-app-password
SMTP_FROM=no-reply@yourdomain.com

# Google API / OAuth (if used for email/Google features)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_ACCESS_TOKEN=
GOOGLE_SENDER=your-email@gmail.com

# Other service keys
# Add any other keys like STRIPE_SECRET, FIREBASE_* etc. below

# Example:
# STRIPE_SECRET=sk_test_...
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
FIREBASE_API_KEY=REPLACE_WITH_FIREBASE_API_KEY
